var total;

// to add questions received from server to the html document
function adder(count,data[]){
    for(i=0;i<count;i++){
        let document = data[i];
        document.getElementById("body").innerHTML += 
    }
}